[build]
  command = "npm run build && cp -r quarto_out/* _site/ || true"
  publish = "_site"

[build.environment]
  NODE_VERSION = "18"

# Custom 404 page
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
